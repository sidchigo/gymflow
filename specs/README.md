# specs/

This directory contains feature specification documents that all GymFlow feature code must strictly follow (per AGENTS.md §4).

Each spec file should be named after the feature it describes, e.g. `schedule-sync.md`, `auth-session.md`, etc.
