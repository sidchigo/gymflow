# evals/

This directory contains the evaluation harness for GymFlow (per AGENTS.md §4).

Each phase of feature development should be verified with `npx tsc --noEmit` and the evaluation scripts placed here.
